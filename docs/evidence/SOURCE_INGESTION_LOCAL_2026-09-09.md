# Source ingestion local evidence — 2026-09-09

- Environment: local macOS workspace, PostgreSQL configured by `.env` at `127.0.0.1:5433`, PGlite test databases
- Baseline: `main@fedaa22` plus an uncommitted dirty worktree
- Evidence status: local implementation evidence only; not remote CI, deployment, external integration or acceptance
- Operator: Codex session for the repository owner
- Snapshot note: 截至 2026-09-09，刚完成的最后一次当前工作树无 `.env` 回归发现 253 项测试：250 passed、0 failed、3 skipped。跳过项是需要 R2/S3 凭据的远程对象存储契约；此前凭据已暴露且尚未轮换，本轮安全地拒绝继续使用。`tsc`、oxlint、production build、OpenAPI lint、32 项 migration checksum 和固定 seed 本地 chaos 10/10 已复核；新增公开网页/平台 Feed、全部有效 origin 图输入、保守关系分类、Social Evidence 最大匹配、unknown/低置信度/social fail closed、不可变人工修正、声明级合格支持、Worker 重算、数据集误独立率/召回率与冻结阈值回归。来源凭据 Broker/OAuth/连接会话实现已从当前 tree 删除；此前敏感值 canary、镜像和 Compose 证据不能自动代表本次修改后的 tree，仍需 DELIVERY-01 重跑。dirty worktree 仍没有可还原 tree/untracked manifest hash，因此本文只能证明本次本地观察，不能支持 Delivered。
- Security incident note: 本轮曾执行会展开 `.env` 的 Compose 配置输出，导致真实 OpenAI 与对象存储凭据出现在工具输出；本文不记录也不回显任何值。相关凭据必须按已暴露处理并立即轮换，旧凭据失效验证是后续交付硬门禁。

## Executed checks

| Test ID | Type | Status | Command/evidence | Executed at | Validity |
| --- | --- | --- | --- | --- | --- |
| SRC-LOCAL-UNIT-001 | historical unit + PG integration + external storage | passed | `npm test` outside the restricted network sandbox; 200 passed, 0 failed, 0 skipped, including configured R2/S3 | 2026-09-09 11:31 +08 | tree before image scan scripts/CI wiring; application/data paths match, but this is not the final tree and the credentials are now treated as exposed |
| SRC-LOCAL-UNIT-002 | current-tree unit + PG integration | partial | safe run without `.env`: 253 discovered, 250 passed, 0 failed, 3 skipped; only configured R2/S3 contract tests skipped | 2026-09-09 | current dirty tree; local/PGlite coverage current, but DELIVERY skip=0 remains blocked until credential rotation and an explicitly authorized rerun |
| SRC-SOCIAL-EVIDENCE-001 | domain + PG integration + API/OAS/UI/static | passed locally | `0030` adds immutable origin corrections and Social Evidence calibration metadata; every active origin of a normalized article feeds the graph and evidence independence uses maximum family↔ownership-group matching; a conservative deterministic classifier recognizes syndication/repost markers and only marks a registered publisher host as original; managed unknown/low-confidence/generated origins and social/web without an approved policy fail closed; corrections validate publisher entities, retain history, are included in source-scoped legal deletion, audit and enqueue source:pipeline recompute; only verified `original` relationships can qualify under an approved policy; project G0/G2 require qualified claim-level support; labelled-set evaluation freezes dataset SHA, false-independent rate ≤2%, recall ≥80% and production sample ≥30 before independent approval can activate a policy | 2026-09-09 | synthetic/local contract only; no authorized labelled dataset, real production sample, evaluation pack or Product/Editorial sign-off; Social Evidence remains In progress |
| SRC-CHAOS-LOCAL-001 | fixed-seed PGlite fault injection | passed locally | `npm run source:chaos` ran seed `signal40-source-chaos-2026-09-09-v1` with 2 schedulers and 3 workers (including protocol v1); 10/10 checks proved occurrence idempotency, one active run, live/backfill serialization, protocol/lease fencing, raw failure replay, page/complete rollback, staged invisibility, no duplicate origin/revision, monotonic checkpoint and one recompute | 2026-09-09 current tree | production process/container SIGKILL, network/clock/storage faults, in-flight deployed rights/config mutations, fixed runtime and SRE/Platform sign-off remain pending; `P1B-CHAOS-01` is not closed |
| SRC-CREDENTIAL-001 | historical, superseded | superseded | 删除前的 credential policy/binding/version/rotation/revocation 测试记录；`0031` 和当前代码已移除该子系统 | 2026-09-09 | 不描述当前 release，不能作为当前能力或安全验收证据 |
| SRC-LEGAL-DELETE-001 | PG integration + Worker/OAS static | passed locally | 5 tests: independent hold release, monotonic hold epoch invalidating a leased withdrawal, fresh-lease recovery, object failure retry, shared normalized article retention, and external withdrawal failure/manual retry/success receipt gate；Render Worker platform-call preflight and worker-only OAS route are wired; targeted source/legal/migration/OAS/control-plane suite passed 37/37 | 2026-09-09 approximately 13:36 +08 | PGlite and in-memory deletion storage; no real platform withdrawal, concurrent provider call, legal authority identity or sign-off |
| SRC-LEGAL-OPERATOR-001 | authorization + PG constraint + API/OAS/UI/static | passed locally | `0029` adds admin-only `can_manage_source_legal`; admin role alone cannot create/release hold, request legal deletion or retry it; hold creation requires another active capable admin and locks the same governance set used by membership demotion; creator self-release and reducing active operators below two during a hold fail closed; governance UI assigns the capability and source UI views/creates/releases holds without a terminal; team-member request/response schemas are explicit | 2026-09-09 approximately 14:26 +08 | PGlite/static UI only; no real identity browser matrix, concurrent HTTP race drill, legal authority directory or Security/Legal sign-off |
| SRC-CONNECTOR-CANARY-001 | release control + PG integration + API/OAS/UI/static | passed locally | `0028` persists connector/version canary state, 1–100% stable source buckets, failure threshold and minimum sample; job payload and run freeze selected versus shadow mode; starting or changing canary cancels queued/retrying work for re-scheduling, while both completion routes re-evaluate the current bucket and quarantine an invalidated leased formal run without pausing its source; non-selected sources cannot write formally; the next Scheduler tick automatically invokes the existing version kill switch at threshold, preserving start/stop times, audit and attention behavior; DB range checks and source-console controls are covered | 2026-09-09 approximately 13:56 +08 | local PGlite and static UI only; no target-environment bucket distribution, real failure injection, queued/leased concurrency, alert routing, recovery or SRE/Platform sign-off; `P0C-DRILL-01` remains open |
| SRC-SLO-001 | unit + PG integration | passed | 7 tests: insufficient/low-frequency samples never healthy; versioned policy freezes eligible schedule terminals and separately reports status/fetch outcomes; audited exclusion windows remove matching scheduled runs without erasing other history; usage/quota/freshness/cost, missed occurrence burn, weighted dimensions and deduplicated alerts | 2026-09-09 12:48 +08 | synthetic 7/28-day history; no signed real-source list, alert-target drill, continuous observation or multi-scheduler chaos |
| SRC-SLO-POLICY-001 | policy + executable report + UI/static | passed locally | `SOURCE_SLO_POLICY` is returned as `/api/v1/operations` `sourceSlo.policy`; `npm run source-slo:report` uses the same query; `/operations` displays the policy version and separately classified outcomes; `tsc`, lint and patch checks pass | 2026-09-09 12:32 +08 | report command was not run against the development DB; Product/SRE approvals remain blank, so this does not close `P1B-SLO-01` |
| SRC-FETCH-OUTCOME-001 | protocol + DB + unit | passed | Worker sends explicit 304 for run-level commit and persists paged outcome in the recovery checkpoint; both completion paths persist `ingestion_runs.fetch_outcome`; shared rules classify `modified/not_modified/unknown`, reject contradictory 304 payloads, and the `0024` DB CHECK rejects unknown enum values; targeted 3/3 plus migration/SLO/control-plane checks passed | 2026-09-09 12:35 +08 | `0024` not applied to development/production PostgreSQL; no real upstream conditional GET run |
| SRC-SLO-EXCLUSION-001 | policy + PG integration + API/UI/static | passed locally | `0025` stores constrained `manual_pause/planned_maintenance` windows; pause requires a reason and enable closes the window; planned maintenance is future-only, ≤7 days, ≤90 days ahead, idempotent and cancellable only before start; cancellation remains audited; SLO excludes matching runs and expected occurrences；来源卡片现可登记、查看和开始前取消计划维护；专项 4/4 且进入 253 项完整回归 | 2026-09-09 approximately 14:26 +08 | migration not deployed; UI 有静态回归，但无真实 operator/browser drill 或 signed observation start |
| SRC-BUDGET-001 | unit + PG integration | passed | policy range/no-price rejection; run reservation and soft warning; atomic hard block plus scheduler occurrence advance; projected month-end/exhaustion and affected article/topic projection, 3/3 | 2026-09-09 03:30 +08 | explicit configured estimates only; no supplier invoice reconciliation; priority throttling is evidenced separately by `SRC-SCHEDULE-THROTTLE-001` |
| SRC-SCHEDULE-THROTTLE-001 | policy + scheduler/PG + SLO + API/OAS + UI/static | passed locally | `0026` adds source priority, opt-out, effective 1x/2x/4x state and immutable skipped-occurrence records; the scheduler preserves original cron, records budget snapshot/recovery time and remains idempotent under a repeated tick; SLO v2 removes only persisted throttle occurrences and reports `budgetThrottled`, while an unrecorded miss still burns error budget; source and operations UI expose policy, multiplier, skipped count and recovery | 2026-09-09 approximately 13:21 +08 | synthetic price/spend and PGlite only; no development/production migration, supplier invoice reconciliation, real budget threshold crossing, browser operator drill or SRE/Product sign-off |
| SRC-OWNERSHIP-001 | unit + PG integration | passed | active role validation, version-guarded audited business-owner transfer, linked critical attention after owner suspension, enable readiness check and automatic resolution after recovery | 2026-09-09 | synthetic members and source; no real employee offboarding drill |
| SRC-OWNERSHIP-DATA-001 | development PG data audit | blocked | post-`0015` read-only query: 1 non-archived draft source, 1 missing business owner/steward, 0 team members; migration correctly did not invent an owner | 2026-09-09 03:47 +08 | add an active admin in `/governance`, transfer the draft in `/sources`, then rerun the ownership completeness SQL before environment acceptance |
| SRC-OWNERSHIP-UI-001 | local browser smoke | passed | current worktree on `localhost:3011`: `/sources` and `/governance` returned 200; visually verified the no-admin guidance/link, disabled create gate, existing draft's unassigned owner/steward state, expandable three-role editor and version-lock explanation | 2026-09-09 03:53 +08 | visual/empty-state smoke only; no member was created and no real ownership transfer/offboarding flow was executed |
| SRC-PAGE-PROTOCOL-001 | PG integration | passed | page replay/hash+epoch conflict, ordinal/checkpoint/final guards, cross-lease completion manifest and actual `(run,page_key)` / `(run,page_ordinal)` unique constraints | 2026-09-09 04:24 +08 | PGlite protocol and constraint evidence; not a real upstream interruption |
| SRC-PAGE-API-001 | local API + development PG | passed | temporary `localhost:3012` control plane: PUT page 0 = 200, identical replay = 200, changed body = 409, page 1 final = 200, complete = 200, run succeeded and checkpoint version = 2; cleanup query returned sources 0/runs 0 | 2026-09-09 04:30 +08 | synthetic metadata-only HTTP JSON source; no external supplier or process-kill recovery |
| SRC-CAPABILITY-PROTOCOL-001 | lease + worker compatibility | passed | protocol v1 Worker with `source:http-json` cannot lease v2 job; v2 Worker can even when the deprecated product-version column contains a nonmatching value; heartbeat/orphan detection checks the same per-capability maximum; live page smoke leased with HTTP JSON protocol v2 | 2026-09-09 04:35 +08 | local/PGlite and development PG only; no mixed production rollout |
| SRC-LEASE-FENCE-001 | worker write isolation | passed | source-test completion, topic recompute and raw upload send and verify current owner/`leaseEpoch`/expiry; shared predicate rejects non-leased state, wrong owner, stale epoch and exact-expiry writes | 2026-09-09 | unit/static/route-contract boundary; no process-kill or multi-worker production chaos |
| SRC-RUN-PAGINATION-001 | PG integration + API contract | passed | opaque `(created_at,id)` cursor round-trip and validation; PGlite proves equal-timestamp rows cross pages without omission/duplication; OpenAPI exposes bounded `cursor`/`limit` | 2026-09-09 04:51 +08 | local contract and PGlite only; no large production-history load test |
| SRC-GOVERNANCE-CONTRACT-001 | API contract + unit | passed | every `/source-*` mutation has a dedicated request schema; shared positive-integer and trimmed text bounds cover archive/cutover/withdrawal/legal-hold actions; targeted boundary test passed | 2026-09-09 | request-side local contract only; governance success schemas were completed later in `SRC-GOVERNANCE-SUCCESS-001`; published compatibility baseline remains pending |
| SRC-ERROR-CONTRACT-001 | API contract | passed | `SourceErrorEnvelope` requires `error` and `errorCode`; every documented source control-plane/ingestion/pipeline non-2xx response binds a source-specific component, critical Worker status sets are frozen, non-source paths have no accidental source-response binding, and OpenAPI/TypeScript error enums remain exactly equal | 2026-09-09 | local documented-response and runtime-construction guard; no published-baseline compatibility diff |
| SRC-SUCCESS-CONTRACT-001 | API contract | passed | source list/create/replay, import preview/commit, detail/update, test queue/read/complete, enable, stable run pagination/enqueue and backfill 2xx responses use dedicated schemas; the contract test fails if a core happy-path response loses its JSON schema | 2026-09-09 09:05 +08 | primary onboarding/ingestion control-plane path; governance responses are covered separately below |
| SRC-BACKFILL-ESTIMATE-001 | unit + API/OAS/UI/static + PG integration | passed locally | source-scoped run detail cannot cross source boundaries; backfill estimate freezes source version/range, item upper bound, conservative request/cost/time values and a SHA-256 confirmation token; large requests reject missing or stale confirmation and the source UI obtains the estimate before confirmation; dedicated request/success/error schemas are linted | 2026-09-09 approximately 14:45 +08 | estimate is based on configured request price/rate and remains an upper-bound planning value; no real supplier quote, invoice reconciliation or browser recording |
| SRC-GOVERNANCE-SUCCESS-001 | API contract | passed | connector registry/policy/release control, ownership, checkpoint cutover, lifecycle actions, withdrawal/deletion/legal hold and quarantine 2xx responses use dedicated schemas | 2026-09-09 | local contract only; target-environment governance acceptance remains pending |
| SRC-WORKER-SUCCESS-001 | API contract | passed | compatible run commit, page commit, recovery, completion and pipeline recompute 200 responses bind their exact dedicated schemas; commit uses a three-way page/run/shadow union | 2026-09-09 | local contract only; no real multi-worker interruption or authorized upstream run |
| SRC-TYPED-ERROR-RUNTIME-001 | API contract + unit + static | passed | all source control-plane, ingestion page/commit/complete/raw and pipeline recompute routes use the shared typed error builder; naked `{error}` scan is empty; status/message mapping, retry metadata and explicit budget code are tested; TypeScript and OpenAPI code sets are exactly equal | 2026-09-09 | runtime construction was covered here; per-path required `errorCode` was completed later in `SRC-ERROR-CONTRACT-001`; no published-baseline compatibility diff |
| SRC-LIFECYCLE-CONTRACT-001 | type + API contract | passed | database schema, source UI type and OpenAPI use or verify the same eight lifecycle states; equality regression rejects enum drift | 2026-09-09 09:19 +08 | local type/contract evidence only; no mixed-version deployment proof |
| SRC-STATE-CONTRACT-001 | DB + type + API + UI | passed | proposal/source lifecycle/health/rights/run/quarantine/release/acceptance families share runtime constants and exact OAS enums; `0019` DB checks reject unregistered values; source manager renders product labels | 2026-09-09 10:45 +08 | local/PGlite contract evidence; no mixed-version deployment proof |
| SRC-RBAC-PROPOSAL-001 | authorization + PG integration + UI/static | passed | exhaustive action×role matrix; researcher/editor own-only proposal list; an admin approves the proposal into a `rights=pending` draft; a distinct active admin with explicit `can_approve_source_rights` capability decides the frozen rights request; requester/self-decision, ordinary-admin decision, checkpoint self-approval and legal-hold self-release fail closed | 2026-09-09 12:16 +08 | no real-identity cross-role browser E2E or Security sign-off |
| SRC-RIGHTS-APPROVAL-001 | authorization + PG integration + API/OAS + UI/static | passed | `0023` adds independent approver capability, provisional request and frozen rights/config/evidence/terms hashes; create/import/proposal paths do not mint grants; approve/reject/resubmit are idempotent and audited; approver must explicitly confirm the frozen `sourceType`; mismatches, unknown dossier fields, stale/self/ordinary-admin decisions fail closed; later config changes do not silently preserve the grant | 2026-09-09 | PGlite/local contract only; real publisher/evidence-family directory mapping, development/production PG upgrade, real dossier artifacts, browser matrix and Legal/Security sign-off remain pending |
| SRC-NET-REGISTRY-001 | security unit + pinned external registry | passed | application policy pins the IANA 2025-10-09 IPv4/IPv6 special-purpose prefixes; every aggregated prefix base, documentation/benchmark/translation ranges, multicast, mapped/NAT64/6to4 private embeddings and adjacent public prefixes are regression-tested | 2026-09-09 12:25 +08 | application-layer address classification only; no target egress policy, proxy/CNAME bypass, actual remote IP/SNI, packet evidence or Security sign-off |
| SRC-RUNBOOK-RESTORE-001 | runbook/static guard | passed | restore example uses the script's real `CONFIRM_RESTORE=isolated` + `RESTORE_TARGET_DB=signal40_restore_*` contract; regression rejects the stale variable, missing prefix guard or overwrite guard | 2026-09-09 12:25 +08 | no restore was executed; development/production isolated restore evidence remains pending |
| SRC-ACTOR-DTO-001 | API contract + projection unit | passed | strict Actor source/test/run/cutover/deletion/proposal allowlists, separate workload-only source config route, sensitive URL query/userinfo/fragment removal and forbidden-field schema scan; adjacent sensitive query regression included | 2026-09-09 10:45 +08 | no real browser HAR/video, target-environment log/trace/snapshot/export scan or Security sign-off |
| SRC-SENSITIVE-CANARY-001 | scanner self-test + Actor DTO + CI static | passed locally | `npm run source:sensitive-canary` generated a one-time marker, proved detection of raw/URL/Base64/Base64URL/hex forms without printing the marker, rejects compressed/binary inputs until unpacked as UTF-8 and scans Actor DTO/redacted artifact surfaces; repeated `--artifact` accepts real HAR/log/trace/snapshot/export text files; CI injects a run-unique marker and wiring is regression-tested | 2026-09-09 | no real artifact was supplied locally and remote CI was not run; browser/target capture plus Security sign-off remain required |
| SRC-VISIBILITY-001 | PG integration + route invariant | passed | page rows store normalized `staged_payload_json`; before complete they neither create origins nor mutate an already-visible article; public checkpoint stays at the run-start snapshot; completion owns materialization, checkpoint CAS/promotion and deterministic recompute enqueue; full 188/188 plus post-CAS 6/6 | 2026-09-09 10:55 +08 | local/PGlite and static route evidence; no real process-kill or multi-Worker chaos |
| SRC-CONTRACT-DELETE-001 | contract + PG integration + route invariant + UI/static | passed | OAS/runtime freeze `NormalizedSourceItem` as strict `upsert`/`tombstone`; HTTP mapping/UI expose kind/updatedAt/deletedAt; tombstone rejects fabricated content/governance fields; `source_item_event_states` records unknown IDs, makes replay idempotent, gives equal-time delete priority, blocks stale upserts and restores only on a strictly newer update; only changed origins enqueue recomputation; included in current 253-test run | 2026-09-09 | local HTTP JSON contract and PGlite only; no real supplier deletion stream, external platform withdrawal or target-environment multi-Worker chaos |
| SRC-WORKLOAD-BOUNDARY-001 | security + static + compose | passed for current code | production source/render require dedicated profile/token, reject known unrelated secrets, `.dockerignore`/allowlist-copy paths and the current three-image CI scanning matrix are asserted; Compose uses explicit environments | 2026-09-09 | target orchestrator/network policy and runtime Secret matrix remain pending |
| SRC-IMAGE-BUILD-001 | historical, superseded | superseded | 删除来源 Broker 前的四镜像本机构建记录 | 2026-09-09 | 不适用于当前三镜像 tree；必须由当前远端 CI 重建、扫描并生成新 digest |
| SRC-IMAGE-CANARY-001 | historical, superseded | superseded | 删除来源 Broker 前的四镜像 layer canary 记录 | 2026-09-09 | 不适用于当前三镜像 tree；远端 Trivy/SBOM/签名仍未执行 |
| SRC-RUNTIME-ENV-001 | historical, superseded | superseded | 删除来源 Broker 前的 Compose 环境矩阵记录 | 2026-09-09 | 不适用于当前 tree；目标运行环境、网络策略和 process/log/HAR 扫描仍待执行 |
| SRC-STORAGE-001 | external object storage | passed | configured S3/R2 put/get/list/delete, multipart complete/abort and batch delete tests | 2026-09-09 03:26 +08 | configured development bucket only; not a legal-delete production drill |
| SRC-TSC-001 | static | passed | `make typecheck` | 2026-09-09 14:45 +08 | current dirty worktree only |
| SRC-LINT-001 | static | passed | `npm run lint` | 2026-09-09 14:45 +08 | current dirty worktree only |
| SRC-BUILD-001 | build | passed | production build from an isolated temporary copy that excluded `.env*`; public web/feed connectors, Social Evidence governance routes, run-detail/backfill, legal hold, proposals, rights decisions, Actor/Worker split, state/RBAC and tombstone contract are present; source Broker routes are absent | 2026-09-09 | local app build only, not a release artifact |
| SRC-OPENAPI-001 | OAS 3.1 + references + compatibility policy | passed | pinned Redocly CLI validates with 0 errors/0 warnings; run-detail and backfill-estimate contracts, strict team-member legal capability request/response, connector canary and rollout-change error, upsert/tombstone and source-rights schemas are included; regression automatically inventories every implemented source-domain route and exported HTTP method, then resolves all local refs; custom checker rejects path/method/status removal, request/response required incompatibility, type/enum narrowing, security weakening and removal before a 90-day deprecation window; self-baseline CLI pass | 2026-09-09 14:45 +08 | checker/CI wiring implemented locally; immutable published baseline, remote required check and artifact digest remain pending |
| SRC-DIFF-001 | patch hygiene | passed | `git diff --check` | 2026-09-09 14:45 +08 | current dirty worktree only |
| SRC-MIGRATE-009 | PostgreSQL migration | passed | applied `0009_living_tag.sql`; second `npm run db:migrate` reported 10 applied and none pending | 2026-09-09 02:16 +08 | configured development DB only |
| SRC-MIGRATE-010 | PostgreSQL migration | passed | applied `0010_natural_micromacro.sql`; second `npm run db:migrate` reported 11 applied and none pending | 2026-09-09 02:33 +08 | configured development DB only |
| SRC-MIGRATE-011-012 | PostgreSQL migration | passed | applied `0011_parched_bloodaxe.sql` and `0012_aromatic_martin_li.sql`; second `npm run db:migrate` reported 13 applied and none pending | 2026-09-09 02:47 +08 | configured development DB only |
| SRC-MIGRATE-013-014 | PostgreSQL migration | passed | applied `0013_clear_cerebro.sql` and `0014_polite_rafael_vega.sql`; second `npm run db:migrate` reported 15 applied and none pending | 2026-09-09 03:19 +08 | configured development DB only |
| SRC-MIGRATE-015 | PostgreSQL migration | passed | applied `0015_milky_stick.sql`; second `npm run db:migrate` reported 16 applied and none pending | 2026-09-09 03:47 +08 | configured development DB only; historical ownership is backfilled only from active members and unresolved rows remain visible to reconciliation |
| SRC-MIGRATE-016 | PostgreSQL migration | passed | applied `0016_wakeful_wendigo.sql`; idempotent rerun succeeded; read-only audit confirmed page ordinals/hash/final/counters, page/job lease epoch and both page unique indexes | 2026-09-09 04:13 +08 | configured development DB only |
| SRC-MIGRATE-017 | PostgreSQL migration | passed | applied `0017_warm_scarlet_spider.sql`; second run reported 18 applied and none pending; read-only audit found integer job protocol and Worker protocol map | 2026-09-09 04:27 +08 | configured development DB only; the next forward migration restores the deprecated compatibility column |
| SRC-MIGRATE-018 | PostgreSQL migration | passed | applied `0018_daffy_rogue.sql`; second run reported 19 applied and none pending; read-only audit found both integer protocol and deprecated write-only compatibility columns | 2026-09-09 04:35 +08 | configured development DB only; compatibility column must be removed only after old control planes exit |
| SRC-MIGRATION-INTEGRITY-001 | manifest + runner + PG integration | passed | `drizzle/checksums.json` covers exact ordered `0000`–`0031` set; file drift/unregistered migration/database hash mismatch fail closed; runner persists checksums; PGlite fresh install and full regressions consume all 32 files | 2026-09-09 | local runner/PGlite only for `0023`–`0031`; no development/production upgrade, remote CI or current production-baseline restore drill |
| SRC-MIGRATE-019-020 | development PostgreSQL upgrade | passed | adopted manifest hashes for legacy `0000`–`0018`, applied `0019_freeze_source_state_contract.sql` and `0020_source_proposals.sql`, then second `npm run db:migrate` reported 21 applied and none pending | 2026-09-09 10:38 +08 | configured development DB 19→21 upgrade only; not production upgrade evidence |
| SRC-MIGRATE-021 | development PostgreSQL upgrade | passed | applied `0021_stage_ingestion_pages.sql`; second `npm run db:migrate` reported 22 applied and none pending | 2026-09-09 10:52 +08 | configured development DB 21→22 upgrade only; not production upgrade evidence |
| SRC-MIGRATE-022 | development PostgreSQL upgrade | passed | applied `0022_source_item_event_state.sql`; second `npm run db:migrate` reported 23 applied and none pending | 2026-09-09 11:08 +08 | configured development DB 22→23 upgrade only; not production upgrade evidence |
| SRC-MIGRATE-023 | development PostgreSQL upgrade | not run | `0023_independent_source_rights.sql` passed PGlite fresh install and regression coverage, but was not applied through the development connection | 2026-09-09 12:16 +08 | process `DATABASE_URL` is unset and the current `.env` contains credentials that must be rotated; no development or production upgrade claim |
| SRC-MIGRATE-024-029 | development PostgreSQL upgrade | not run | fetch outcome、SLO 排除、预算降频审计、legal-hold epoch、connector canary 和独立法律操作 capability 六项迁移已通过 PGlite fresh install/checksum/专项回归，但未使用开发连接执行 | 2026-09-09 14:26 +08 | process `DATABASE_URL` is unset and the current `.env` contains credentials that must be rotated; no development or production upgrade claim |
| SRC-MIGRATE-030 | development PostgreSQL upgrade | not run | Social Evidence 人工修正与冻结策略迁移已通过 PGlite fresh install/checksum/专项回归，但未使用开发连接执行 | 2026-09-09 | 当前 `.env` 中既有外部凭据须先轮换；无开发或生产升级声明 |
| SRC-MIGRATE-031 | development PostgreSQL upgrade | not run | 来源凭据/连接会话删除与公开 connector release 迁移已通过 PGlite fresh install/checksum/完整回归，但未使用开发连接执行 | 2026-09-09 | 这是有意删除过度设计的 contract migration；仍无开发或生产升级声明 |

The latest current-tree run discovered 253 tests: 250 local/PG tests passed and only the 3 credentialed R2/S3 contracts skipped. In addition to the prior source invariants, it covers public web/feed connectors, complete active-origin graph input, conservative relationship classification, Social Evidence maximum matching, managed-origin fail-closed behavior, immutable manual correction, claim-level qualified support, recompute enqueueing, labelled-set error/recall metrics and frozen approval thresholds. The earlier image/chaos/storage evidence remains historical evidence, not current-tree skip=0 delivery evidence. The local seeded drill is not a real process/container, network, remote object-store or authorized-upstream chaos run.

## Migration checksums

The authoritative manifest is `drizzle/checksums.json`. It contains all 32 files from `0000` through `0031`; copying selected hashes into this evidence document would create a second, drift-prone authority. `npm run db:migrations:verify` is the reproducible check.

## Explicit exclusions

- No browser E2E, real RSS/Public JSON subscription upstream, real network interruption/Worker process kill recovery, real employee offboarding/ownership recovery, real YouTube/platform deletion, opaque supplier cursor, DNS rebinding environment, target-environment multi-scheduler/worker chaos, object-lock retention drill, supplier invoice reconciliation/priority throttling, target container identity/network policy, remote SBOM/Trivy artifact, real browser HAR/video or target-runtime API/log/trace/snapshot/export canary scan, authorized Social Evidence labelled set/production sample, or 28-day observation was executed. The synthetic local canary and fixed-seed PGlite harness are prerequisite evidence only.
- Local image digests exist, but no commit, remote CI run, registry artifact, signature/provenance, deployment or acceptance approval exists for this dirty worktree.
- The exposed development OpenAI/object-storage credentials have not been rotated or independently verified revoked in this session; they must not be treated as safe for continued use.
- Therefore this record supports `Implemented locally` only for the specific checks and capabilities listed above. Foundation as a whole remains `In progress`; this record does not support `Delivered`, `Deployed`, `Integrated`, `Accepted` or Foundation GA.
